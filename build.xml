<project name="overtone" default="plugin">
  <description>Builds the ${ant.project.name} plugin.</description>

  <property name="src.dir"     value="src"/>
  <property name="build.dir"   value="build"/>
  <property name="classes.dir" value="${build.dir}/classes"/>
  <property name="jar.dir"     value="${build.dir}/jar"/>
  <property name="tests.dir"   value="test"/>
  <property name="lib.dir"     value="lib"/>

  <available property="hasclojure" file="${clojure.jar}"/>

  <path id="classpath">
    <path location="${build.dir}"/>
    <path location="${source.dir}"/>
    <fileset dir="${lib.dir}">
      <include name="*.jar"/>
    </fileset>
  </path>

  <target name="clean" description="Remove generated files and directories.">
    <delete dir="build"/>
  </target>

  <target name="init" depends="clean">
    <tstamp/>
    <mkdir dir="${build.dir}"/>
  </target>

  <target name="has-clojure"
    description="Print a warning message if clojure.jar is undefined"
    unless="hasclojure">
    <echo>WARNING: You have not defined a path to clojure.jar so I can't compile files.
      This will cause some parts of clojure.contrib not to work (e.g., pretty print).
      To enable compiling, run "ant -Dclojure.jar=&lt;...path to clojure.jar..&gt;"
    </echo>
  </target>

  <target name="test" description="Run tests">
    <java fork="true" classname="clojure.lang.Script">
      <classpath>
          <path refid="classpath"/>
          <path location="."/>
      </classpath>
      <arg value="${tests.dir}/run.clj"/>
    </java>
  </target>

  <target name="compile" depends="init"
    description="Compile the clojure files that have gen-class."
    if="hasclojure">
    <java classname="clojure.lang.Compile">
      <classpath>
        <path location="${build.dir}"/>
        <path location="${src.dir}"/>
        <path location="${clojure.jar}"/>
      </classpath>
      <sysproperty key="clojure.compile.path" value="${build.dir}"/>
      <arg value="com.demandevolution.demystifier"/>
    </java>
  </target>

  <target name="jar" depends="compile">
    <jar jarfile="build/${ant.project.name}.jar">
      <fileset dir="${src.dir}" includes="**/*.clj"/>
      <fileset dir="${build.dir}" includes="**/*.class"/>
      <manifest>
        <attribute name="Class-Path" value="."/>
      </manifest>
    </jar>
  </target>

  <target name="plugin" depends="jar" description="Create the full plugin directory.">
    <mkdir dir="build/${ant.project.name}"/>
    <copy file="build/${ant.project.name}.jar" todir="build/${ant.project.name}"/>
    <copy file="plugin.ini" tofile="build/${ant.project.name}/${ant.project.name}.ini"/>
    <copy todir="build/${ant.project.name}">
      <fileset dir="lib"/>
    </copy>
  </target>
</project>
